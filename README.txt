
using git
1)install git
2)configure git
3)initialise git ----> asking git to track all directories
4)add a file and make some changes
5)move  it  to  stagging area  , here staging area is imaginry path between working directory and local directory
6)commit them using commit command
 #Branches

1) create A new  branch
		git branch  branchname
	or
	if you  want to checkout to the branch then use
		git checkout branchname
2)changes -> add to staging -> commit
3) if you want merge 
